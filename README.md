# Uber Travel GitHub Pages App

Search cities, display top 5 nearby locations using OpenStreetMap, and show images from Unsplash.

Run `npm install`, then `npm start`, and `npm run deploy` to publish.